    </main>
    <footer class="site-footer">
      <div class="wrap footer-copy">
        Prisha Home Care supports families with trusted home nursing, compassionate professionals, and clear
        care coordination.
      </div>
    </footer>
    <div id="imagePreviewModal" class="image-modal" aria-hidden="true">
      <button type="button" class="close-btn" onclick="closeImagePreview()" aria-label="Close image preview">&times;</button>
      <div class="image-container">
        <img id="previewImage" alt="Preview" />
        <div class="watermark-layer" aria-hidden="true">
          <% for (let index = 0; index < 6; index += 1) { %>
            <span class="watermark-text">Prisha Home Care</span>
          <% } %>
        </div>
      </div>
    </div>
    <a
      href="https://wa.me/919138913355?text=Hello%20I%20need%20help%20from%20Prisha%20Homecare"
      class="whatsapp-float"
      target="_blank"
      rel="noopener noreferrer"
    >
      💬 Help
    </a>
    <style>
      .image-modal {
        display: none;
        position: fixed;
        z-index: 9999;
        inset: 0;
        background: rgba(7, 16, 26, 0.88);
        padding: 2rem;
      }

      .image-modal.is-open {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .image-modal .close-btn {
        position: absolute;
        top: 18px;
        right: 24px;
        border: 0;
        background: transparent;
        color: #fff;
        font-size: 2.4rem;
        line-height: 1;
        cursor: pointer;
      }

      .image-container {
        position: relative;
        text-align: center;
        max-width: min(92vw, 1100px);
        width: fit-content;
      }

      .image-container img {
        display: block;
        max-width: 100%;
        max-height: 80vh;
        border-radius: 18px;
        box-shadow: 0 24px 70px rgba(0, 0, 0, 0.35);
      }

      .watermark-layer {
        position: absolute;
        inset: 0;
        border-radius: 18px;
        overflow: hidden;
        pointer-events: none;
        mix-blend-mode: multiply;
      }

      .watermark-text {
        position: absolute;
        color: rgba(4, 13, 24, 0.46);
        font-size: clamp(18px, 1.9vw, 26px);
        font-weight: 800;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        white-space: nowrap;
        transform: translate(-50%, -50%) rotate(-28deg);
        text-shadow: 0 1px 0 rgba(255, 255, 255, 0.18);
      }

      .watermark-text:nth-child(1) {
        top: 18%;
        left: 20%;
      }

      .watermark-text:nth-child(2) {
        top: 20%;
        left: 76%;
      }

      .watermark-text:nth-child(3) {
        top: 45%;
        left: 50%;
      }

      .watermark-text:nth-child(4) {
        top: 68%;
        left: 22%;
      }

      .watermark-text:nth-child(5) {
        top: 70%;
        left: 78%;
      }

      .watermark-text:nth-child(6) {
        top: 86%;
        left: 52%;
      }

      @media (max-width: 640px) {
        .watermark-text {
          font-size: clamp(14px, 3.8vw, 18px);
        }

        .watermark-text:nth-child(1) {
          top: 18%;
          left: 28%;
        }

        .watermark-text:nth-child(2) {
          top: 30%;
          left: 72%;
        }

        .watermark-text:nth-child(3) {
          top: 46%;
          left: 34%;
        }

        .watermark-text:nth-child(4) {
          top: 58%;
          left: 72%;
        }

        .watermark-text:nth-child(5) {
          top: 74%;
          left: 30%;
        }

        .watermark-text:nth-child(6) {
          top: 86%;
          left: 68%;
        }
      }

      [data-preview-image] {
        cursor: zoom-in;
      }
    </style>
    <% const assetVersion = typeof res !== "undefined" && res.locals && typeof res.locals.assetVersion !== "undefined" ? res.locals.assetVersion : (process.env.NODE_ENV === "production" ? "1.0.0" : Date.now()); %>
    <script src="/app.js?v=<%= assetVersion %>"></script>
  </body>
</html>
