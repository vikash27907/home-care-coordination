<%
const resolvedProfileFormId = (typeof profileFormId === "string" && profileFormId.trim())
  ? profileFormId.trim()
  : "profileForm";
%>

<%- include("../partials/nurse-qualification-module", { nurse, profileFormId: resolvedProfileFormId }) %>
